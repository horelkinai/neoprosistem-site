# AGENTS.md — инструкции для будущих чатов

## Важно
- Это единственный рабочий каталог проекта. Каталог-сосед `img-20260807-205154-968-jpg-640-3` **пустой**, работать нужно здесь.
- Клиент просил: **не продолжать проект в других чатах** — все правки только в этой папке и этом проекте.
- Перед любыми правками прочитайте `PROJECT_INFO.md` — там полное описание проекта.

## Быстрый старт
1. `cd /root/Documents/Codex/2026-08-07/https-chat-deepseek-com-share-jpgfbrvs24jhsftugx-2`
2. Локальный сервер: `python3 -m http.server 8080 --bind 127.0.0.1`
3. Сайт: `http://127.0.0.1:8080`

## Стек
- Чистые HTML/CSS/JS, без сборщиков. Один CSS-файл `css/styles.css`, один JS-файл `js/main.js`.
- Python-скрипты: `tools/gen_pages.py` (пересборка подстраниц), `make_seo.py` (SEO).
- Шрифты Google Fonts; цвета: `#009D9E` (бирюза), `#F9A758`, `#FF5959`.

## Правила правок
- После изменения секций `index.html` перегенерируйте подстраницы: `python3 tools/gen_pages.py`.
- После изменения SEO: `python3 make_seo.py`.
- Не удаляйте старые CSS-блоки hero, не проверив медиазапросы: новые стили героя (большой портрет основателя, ~строка 2172+) наслаиваются поверх старых (~строка 1760+) намеренно.
- Лайтбокс сертификата: все кнопки `[data-open-cert]` открывают `#certLightbox`; маркер лайтбокса есть на каждой странице перед `</body>`. Не создавайте фейковых ссылок на сертификат.
- Мобильное меню — слайд-панель справа (детали в `PROJECT_INFO.md`). Не удаляйте `moveNavForMobile()`/`syncDrawer()` в `js/main.js` и z-index-стек (шапка 100, панель 96, оверлей 95, лайтбокс 1000) — без них панель «уплывает» из-за `backdrop-filter` шапки.
- Поддерживайте стиль neoteh.by: светлая тема, бирюза, аккуратные карточки, адаптив.

## Проверка
- Все страницы и ассеты должны отдавать 200: `curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8080/index.html`
- Вёрстку проверяйте скриншотами Playwright (десктоп ~1280px, мобильная ~390px), включая лайтбокс и герой.
- Проверяйте консоль браузера: ошибок быть не должно.

## Контакты (не выдумывать другие)
- Telegram `@ukol_off`, Instagram `@neoprosistem`
- BY: `+375 29 108-88-90`, RU: `+7 910 334-78-51`
- Канонический домен: `https://neoprosistem.by/`, производство: `https://neoteh.by`
